<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Jc.PopupView.Avalonia.Sample.ViewModels"
             xmlns:popup="https://github.com/jcsawyer"
             xmlns:sheets="clr-namespace:Jc.PopupView.Avalonia.Sample.Views.Sheets"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Jc.PopupView.Avalonia.Sample.Views.MainView"
             x:DataType="vm:MainViewModel"
             TopLevel.AutoSafeAreaPadding="False">
    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
         to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <vm:MainViewModel />
    </Design.DataContext>
    <Grid Background="#fbf8fd">
        <popup:DialogHost>
            <StackPanel Spacing="20" Margin="20">
            <TextBlock Classes="h1">Action sheets</TextBlock>
            <!-- Pill Internal Sheet -->
            <Button Classes="panel" Command="{Binding OpenSheet1Command}">
                <Button.Transitions>
                    <Transitions>
                        <DoubleTransition Property="Opacity" Duration="0:0:0.1"></DoubleTransition>
                    </Transitions>
                </Button.Transitions>
                <Grid ColumnSpacing="20" RowSpacing="10" ColumnDefinitions="Auto, *">
                    <Border Grid.Column="0" Classes="icon">
                        <Grid RowDefinitions="*, Auto">
                            <Border HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="40"
                                    CornerRadius="5 5 10 10" />
                        </Grid>
                    </Border>
                    <Grid Grid.Column="1" RowDefinitions="Auto, Auto" ColumnDefinitions="*" VerticalAlignment="Center">
                        <TextBlock Grid.Row="0" Classes="h2" Text="Action sheet one" />
                        <TextBlock Grid.Row="1" Classes="p" Text="Action sheet that fits to the full size of the screen." />
                    </Grid>
                </Grid>
            </Button>
            <!-- Pill External Sheet -->
            <Button Classes="panel" Command="{Binding OpenSheet2Command}">
                <Button.Transitions>
                    <Transitions>
                        <DoubleTransition Property="Opacity" Duration="0:0:0.3"></DoubleTransition>
                    </Transitions>
                </Button.Transitions>
                <Grid ColumnSpacing="20" RowSpacing="10" ColumnDefinitions="Auto, *">
                    <Border Grid.Column="0" Classes="icon">
                        <Grid RowDefinitions="*, Auto">
                            <Border Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="3"
                                    Width="10" CornerRadius="1" Margin="0 0 0 2" />
                            <Border Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="30"
                                    CornerRadius="5 5 10 10" />
                        </Grid>
                    </Border>
                    <Grid Grid.Column="1" RowDefinitions="Auto, Auto" ColumnDefinitions="*" VerticalAlignment="Center">
                        <TextBlock Grid.Row="0" Classes="h2" Text="Action sheet two" />
                        <TextBlock Grid.Row="1" Classes="p"
                                   Text="Action sheet where the drag indicator pill is on the outside of the content panel." />
                    </Grid>
                </Grid>
            </Button>
            <!-- Dynamic Sheet -->
            <Button Classes="panel" Command="{Binding OpenSheet3Command}">
                <Button.Transitions>
                    <Transitions>
                        <DoubleTransition Property="Opacity" Duration="0:0:0.3"></DoubleTransition>
                    </Transitions>
                </Button.Transitions>
                <Grid ColumnSpacing="20" RowSpacing="10" ColumnDefinitions="Auto, *">
                    <Border Grid.Column="0" Classes="icon">
                        <Grid RowDefinitions="*, Auto">
                            <Border HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="40"
                                    CornerRadius="5 5 10 10" />
                        </Grid>
                    </Border>
                    <Grid Grid.Column="1" RowDefinitions="Auto, Auto" ColumnDefinitions="*" VerticalAlignment="Center">
                        <TextBlock Grid.Row="0" Classes="h2" Text="Dynamic action sheet" />
                        <TextBlock Grid.Row="1" Classes="p"
                                   Text="Action sheet that is loaded dynamically." />
                    </Grid>
                </Grid>
            </Button>
        </StackPanel>
            <popup:DialogHost.Sheets>
                <popup:Sheet IsOpen="{Binding IsSheet1Open, Mode=TwoWay}" ClickOutsideToDismiss="True">
                    <sheets:Sheet1 />
                </popup:Sheet>
                <popup:Sheet IsOpen="{Binding IsSheet2Open, Mode=TwoWay}" PillLocation="External">
                    <sheets:Sheet1 />
                </popup:Sheet>
            </popup:DialogHost.Sheets>
        </popup:DialogHost>
    </Grid>
</UserControl>